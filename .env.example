# Anonamoose Environment Configuration
# Copy this file to .env and fill in your values
# For Coolify deployment, set these in the Coolify UI instead

# Required: Token for accessing stats dashboard
STATS_TOKEN=changeme

# Optional: SQLite database path (defaults to ./data/anonamoose.db)
ANONAMOOSE_DB_PATH=

# Optional: Token for management API (if set, protects /api/v1/* endpoints)
API_TOKEN=

# Optional: Override external ports (local docker-compose only)
# PROXY_PORT=3100
# MGMT_PORT=3101
# UI_PORT=3102
