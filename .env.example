# Anonamoose Environment Configuration
# Copy this file to .env and fill in your values

# Required: Token for accessing stats dashboard
STATS_TOKEN=changeme

# Optional: Token for management API (if set, protects /api/v1/* endpoints)
API_TOKEN=

# Optional: Your LLM API keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Ports (default - these are internal container ports)
# External ports are configured in docker/docker-compose.yml
# Default external: 3100 (proxy), 3101 (management), 3102 (dashboard)
